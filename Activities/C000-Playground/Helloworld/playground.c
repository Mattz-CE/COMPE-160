#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

int main()
{
	int x, y, a, s, d, l, k, j;

	scanf("%i %d", &x, &y);
	scanf("%i %d", &a, &l);
	scanf("%i %d", &s, &k);
	scanf("%i %d", &d, &j);

	printf("%d %d\n", x, y);
	printf("%d %d\n", a, l);
	printf("%d %d\n", s, k);
	printf("%d %d\n", d, j);
	return 0;
}